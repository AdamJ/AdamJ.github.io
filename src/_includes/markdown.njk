---
layout: base.njk
---

<section class="hero">
  <h1>{{ title }}</h1>
  <p class="text-lead mb-4">
    {{ description }}
  </p>
  {%- if (headerButton) -%}
  <a href="{{ primaryURL}}" class="btn btn-secondary">{{ primaryAction }}</a>
  {%- else -%}{%- endif -%}
</section>

<section>
  {{ content | safe }}
</section>
